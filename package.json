{
  "name": "kstat_test",
  "version": "1.0.0",
  "description": "A test case for the new version of Kalastatic.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "build": "node main.js && cp -R assets/ build/"
  },
  "kalastatic": {
    "build_directory": "build",
    "pages_directory": "pages",
    "assets_directory": "assets",
    "namespaces": {
      "templates": "templates",
      "components": "components",
      "library": "library"
    }
  },
  "dependencies": {
    "twig": "^1.16.0",
    "drupal-twig-extensions": "^1.0.0-beta.4",
    "sass": "^1.58.3"
  }
}
