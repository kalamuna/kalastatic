<html>
  <head>
    <title>{{ title }}</title>
    {{ stylesheets }}
  </head>
  <body>
    {% block body %}
    {% endblock body %}

    {# Output the stylesheets again afer the body block until we figure out hoisting issue.#}
    {{ stylesheets }}
    {{ scripts }}
  </body>
</html>
